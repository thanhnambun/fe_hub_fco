/** Thời gian (ms) mặc định coi query là “fresh” — 1 phút, khớp product UX phổ biến. */
export const DEFAULT_QUERY_STALE_TIME_MS = 60_000;

/** Số lần retry mặc định cho query lỗi mạng tạm thời. */
export const DEFAULT_QUERY_RETRY_COUNT = 1;
